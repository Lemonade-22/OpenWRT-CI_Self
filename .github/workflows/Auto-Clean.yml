# SPDX-License-Identifier: MIT
# Copyright (C) 2026 Lemonade-22

name: Auto-Clean

on:
  # 每日 05:00（GMT+8）先构建 N60 Pro 512m 和 XG-040G 系列，再清理旧版本
  schedule:
    - cron: '0 21 * * *'
  workflow_dispatch:

permissions: write-all

jobs:
  build-n60:
    name: Build N60 Pro 512m
    uses: ./.github/workflows/N60-Pro-512m.yml
    secrets: inherit

  build-xg:
    name: Build XG-040G Series
    uses: ./.github/workflows/XG-040G-Series.yml
    secrets: inherit

  cleanup:
    name: Clean previous releases and workflow runs
    needs:
      - build-n60
      - build-xg
    runs-on: ubuntu-latest
    steps:
      - name: Delete old Releases and Workflows
        uses: ophub/delete-releases-workflows@main
        with:
          gh_token: ${{ secrets.GITHUB_TOKEN }}
          # 今日保留 3 个 Release：N60 WIFI-NO、N60 WIFI-YES、XG-040G 系列四设备
          releases_keep_latest: 3
          delete_releases: true
          delete_tags: true
          delete_workflows: true
          workflows_keep_day: 1
