# Panther X2 / RK3566: native ImmortalWrt target with vendored LEDE support.
CONFIG_TARGET_rockchip=y
CONFIG_TARGET_rockchip_armv8=y
CONFIG_TARGET_DEVICE_rockchip_armv8_DEVICE_panther_x2=y
CONFIG_TARGET_KERNEL_PARTSIZE=128
CONFIG_TARGET_ROOTFS_PARTSIZE=1024
CONFIG_TARGET_ROOTFS_EXT4FS=n
# Preserve this repository's wired-only Panther profile.
# The imported DTS describes optional wireless hardware; no wireless packages
# are added by this board profile. Hardware operation requires device testing.
